//This controller is currently deprecated; only here to hold code I am not using but was messing with previously.



app.controller('navController', function navController($location, $scope) {
  // $scope.selectedAlbum = "";

  // $scope.go = function go(page) {
  //     console.log($scope.selectedAlbum);
  //   $location.path('/' + $scope.selectedAlbum);
  //   };
});

    // $scope.getAlbum = function(albumID) {
    // return $http.get(baseUrl + "/" + albumID)
    //         .then(function(response) {
	// 			$scope.albumData = response.data;
	// 			console.log($scope.albumData);
	//
    //         });
    // };
